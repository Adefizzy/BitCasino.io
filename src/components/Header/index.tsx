import { StyledHeader, StyledLogo} from './style';


const Header = () => {
  return (
    <StyledHeader>
      <StyledLogo src='/assets/logo.svg' alt='bitcasino' />
    </StyledHeader>
  );
};

export default Header;
